/**
 * Configuración de formulario para PERMISOS — DEPRECATED
 *
 * Los permisos son read-only desde la UI. Se gestionan via SQL seeds.
 * Este archivo se mantiene vacío para no romper imports residuales.
 * Si no hay imports residuales tras el refactor, puede eliminarse.
 */
export const permisosFormFields = [];
export const permisosValidation = {};
export const permisosModalConfig = {};
