{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "vite",
  "devCommand": "vite --port $PORT",
  "rewrites": [
    {
      "source": "/((?!api/|@|.*\\..*$).*)",
      "destination": "/index.html"
    }
  ]
}
